<%= form_for @report do |f| %>
 	<%= f.label :date %><br>
    <%= f.text_area :date %> <br>
	<% @food.each do |food| %>
		<%= check_box_tag "foods[]", food.id %> <%= food.name %><br>
	<% end %>
	<%= f.submit %>
<% end %>
